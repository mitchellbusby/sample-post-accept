{
  "name": "post-accept-sample",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.2.1"
  },
  "scripts": {
    "start": "parcel src/index.html",
    "build": "yarn clean && parcel build src/index.html --dist-dir ./docs --public-url /sample-post-accept/",
    "clean": "rm -rf docs"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
